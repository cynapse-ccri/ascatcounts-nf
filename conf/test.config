docker {
    enabled = true
    // to prevent issues in github-action
    runOptions = '-u $(id -u ${USER}):$(id -g ${USER})'
}

params {
    // add any params required for testing
    hello = 'world'
}
